#include <iostream>
using namespace std;

int main() {
	int n, r;
	cin >> n >> r;
	for (int i = 0; i < n; i++)
	{
		int x, y;
		cin >> x >> y;
		cout << x << ' ' << y << endl;
	}
	/*因為題目後面有提到解不唯一，代表只要符合題目給定的公式就好
	  因此直接輸出測資給的輸入值也可以過*/
}